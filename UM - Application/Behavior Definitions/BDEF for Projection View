projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_STUDENT_INFO alias Student
use etag
{
  use create;
  use update;
  use delete;

  use action BehaviorRating;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;


  use association _Result { create; with draft; }
}

define behavior for ZC_STUDENT_RSLT alias Result
use etag
{
  use update;
  use delete;

  use association _Student { with draft; }
}
