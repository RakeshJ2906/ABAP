projection;
strict ( 2 );
use draft;

define behavior for ZC_SHIPMENT alias Shipment
{
  use create;
  use update;
  use delete;

  use function GetDefaultsForWeather;
  use action assign_delvdate;
  use action delv_prediction;

  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
  use action Prepare;
}
